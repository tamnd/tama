// Layout breakpoints for JS logic. Media queries cannot read custom
// properties, so this value duplicates the breakpoint documented in the
// styles/tokens.css header comment; the breakpoints test parses that
// comment and fails if the two drift.

/** Below this width the nav rail becomes the bottom tab bar. */
export const BP_MOBILE = 700
